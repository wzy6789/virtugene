# 更新日志

本项目的所有版本变更都记录在此文件中。

版本格式遵循 [语义化版本](https://semver.org/lang/zh-CN/)：`主版本.次版本.修订号`。

---

## [1.1.1] - 2026-08-17

### 修复
- **修复用户消息被重复发送**：对话请求中模型会看到同一条用户消息两遍，影响回复质量
- **修复聊天请求无超时**：网络挂起时不再无限显示「对方正在输入」，超时提示「基因链接超时，请重试」
- **修复长会话加载卡顿**：进入会话只加载最近 200 条消息，可手动「加载更早的消息」；角色预览不再全量读取会话
- **修复 AI 回复串会话**：回复生成期间切换到其他角色/会话，回复不再出现在错误的对话里
- **修复「对方正在输入」被遮挡**：发送后输入指示始终可见；切换会话不再被旧回复锁定输入
- **修复情绪自动结算**：触发计数修正（严格每 5 条用户消息分析一次）；结算完成新增「情绪图谱已更新」轻提示
- **修复好感度并发丢更新**：结算/好感度写库改为 IndexedDB 事务串行，不再互相覆盖
- **修复主动消息心情下滑被覆盖**：心情改为增量更新，bump 真正生效
- **好感度可降但不退阶**：数值可以下降，但关系境界（等级）只升不降
- **修复括号清理误删**：不再误删含数字、字母、标点的合法括号内容（如 `(2024)`、公式）
- **修复侧栏滚动条与拉伸冲突**：滚动条贴右缘 8px，拉伸改为右下角把手
- **断开灵魂链接增加二次确认**，防止误退出

### 新增
- **应用内「流体云」消息提醒**：角色主动消息或回复到达其他会话时，界面顶部弹出毛玻璃云朵，点击跳转对应会话
- **回复质量自检**：空回复 / 复述用户 / 通用客服腔 / 自我重复自动静默重试，并附带人设修正提示
- **长会话滚动摘要**：早期对话自动压缩成摘要，角色能"记得"几天前的话题
- **上下文增强**：时间感知（现在几点、距上次聊天多久）、关系状态文字化、用户情绪感知注入
- **动态温度**：按角色主动倾向微调回复风格（高冷更克制、活泼更跳脱）
- **12 位预设角色**补充固定称呼与情绪行为，自定义角色生成模板同步要求
- **发送失败重试**：失败消息显示红色感叹号，点击原样重发（微信式）
- **分条消息逐条延迟发出**，模拟真人打字节奏；回复被截断自动补「…」

### 优化
- 情绪 + 记忆 + 好感度结算合并为单次 API 调用，减少约 30% 额外消耗
- 长期记忆注入上限 15 条并与已有记忆去重，避免上下文膨胀
- 侧边栏预览与未读数并行加载，角色多时启动更快
- 回复 max_tokens 500 → 1000
- 界面拟真：输入框「发消息…」、删除消息二次确认、空会话「聊点什么吧」、微信/QQ 式左侧工具条布局、「基因实验室」入口上移

---

## [1.0.1] - 2026-08-13

### 修复
- **修复多账号数据隔离缺陷**：会话、消息、记忆、好感度/心情此前未按用户隔离，登录另一账号会看到前一账号的数据。现为 `sessions` / `memories` / `characterStates` 增加 `userId`，所有读写按当前用户过滤
- 修复「注销账号」未清除记忆（`memories`）与情绪快照（`emotionSnapshots`）两张表
- 修复退出登录后残留上一账号的界面数据（内存态未重置）

### 数据库
- IndexedDB 迁移 v7 → v9：新增 `userId` 索引与复合主键；会话级数据一次性清空（账号与角色定义保留，预设角色自动重建）

---

## [1.0.0] - 2026-08-13

VirtuGene 首个正式版本 —— *Unlock Your Digital Soul.*

一款本地优先的桌面「数字灵魂」伴侣应用。基于 Electron 构建，所有数据（账号、角色、对话、记忆、情绪）均加密存储于本地 IndexedDB，API Key 由用户自持，绝不落盘明文。

### 新增

#### 账号与安全
- 注册 / 登录 / 注销，账号与 DeepSeek API Key 一对一绑定
- 注册时在线校验 Key 有效性，无效当场拒绝
- 密码使用 **PBKDF2（100,000 次迭代 + SHA-256）** 单向哈希存储
- API Key 使用 **AES-GCM** 加密存储，明文仅驻留内存
- 设置页：脱敏查看 Key、更换 Key、注销账号（二次确认）

#### 角色系统
- 3 个预设角色：林霜（理性毒舌）、艾莉（开朗浪漫）、苏格拉底（哲学猫）
- 自定义角色创建 / 编辑 / 删除（预设角色不可删除）
- **AI 辅助生成角色**：输入描述即可生成人设，支持联网搜索与 PDF / Word / TXT 参考文档导入
- **基因库**：预设 / 共享 / 我的 分类筛选 + 关键词搜索
- 角色发布共享，其他用户可在基因库发现并使用

#### 会话与对话
- 会话创建 / 重命名 / 删除 / 未读角标
- 微信风格口语化回复（`deepseek-v4-flash`），带烟火气、拒绝「通用机器人腔」
- 多轮上下文（最近 20 条，10 轮对话）
- 单次回复可拆分为多条消息（`---` 分隔）
- 完整错误处理：401 无效 Key / 402 余额不足（带充值跳转）/ 429 频繁请求 / 5xx 服务中断

#### 记忆与情绪
- 每 10 轮对话自动提取「长期记忆」，并注入后续回复上下文
- 六维情绪模型：愉悦度 / 唤醒度 / 亲密度 / 投入度 / 外显度 / 稳定度
- 雷达图可视化 + 历史情绪波动追踪
- 好感度 / 心情 动态数值，真实影响回复语气（不直接显示数字）

#### 主动消息
- 根据角色性格倾向（proactivity）主动发起对话，融入当前好感度与心情

#### 界面与体验
- VirtuGene 品牌视觉（基因紫 `#6C5CE7` / 生命青 `#00CEC9`）
- 深色 / 浅色主题切换
- 无边框窗口 + 自定义标题栏（拖拽 / 最小化 / 最大化 / 关闭）

### 修复
- 修复打包后应用无法显示登录界面（开发/生产环境判断错误，误连 dev server）
- 修复打包后页面黑屏（构建资源使用绝对路径，`file://` 下加载失败，改为相对路径）

### 技术栈
- **桌面框架**：Electron 35（主进程 API 代理，`ipcMain` + `net`）
- **前端**：React 19 + TypeScript 5.8 + Tailwind CSS 3
- **状态管理**：Zustand 5
- **本地存储**：Dexie.js 4（IndexedDB），含版本化迁移（v1 → v7）
- **打包**：electron-builder 26（Windows NSIS / macOS DMG）

---

[1.1.1]: https://github.com/wzy6789/virtugene/releases/tag/v1.1.1
[1.0.1]: https://github.com/wzy6789/virtugene/releases/tag/v1.0.1
[1.0.0]: https://github.com/wzy6789/virtugene/releases/tag/v1.0.0
